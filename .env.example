# Copy this file to .env.local and replace the SESSION_SECRET value

# Required: Used to encrypt user session cookies
# Generate a secure value with: openssl rand -base64 32
SESSION_SECRET=replace-this-with-a-long-random-string-at-least-32-chars